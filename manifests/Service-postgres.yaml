apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  labels:
    app: postgres
#    service: sts-mysql-svc
spec:
  ports:
  - port: 5432
    protocol: TCP
    targetPort: 5432
    name: tcp
  type: ClusterIP
  selector:
    app: postgres
